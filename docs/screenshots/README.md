# Docs screenshots

Mobile replay screenshots committed for documentation. Regenerate with:

```bash
npm run docs:snapshots
```

Source flow: `npm run screenshot:replay` (hermetic; see `docs/AGENTS.md`).
