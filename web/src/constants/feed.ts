/** Bills per page in the chronological timeline (mobile + desktop). */
export const FEED_PAGE_SIZE = 15

/** Max page size accepted by `/feed/latest.json` (worker `FEED_MAX_PAGE_SIZE`). */
export const FEED_MAX_PAGE_SIZE = 50
